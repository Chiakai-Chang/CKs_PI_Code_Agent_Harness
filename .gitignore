# Runtime / local-only
pi-config/auth.json
pi-config/settings.json
pi-config/models.json
pi-config/model.json
pi-config/external-skills-manifest.json
# generated by skill-namespace-guard at session start, contains machine paths
pi-config/skill-conflict-report.json
# generated by restore.py when skillTiers.mode is "tiered"; contains machine paths
pi-config/skill-catalog.json
.superpowers/
.understand-anything/
# async-exec-bridge job records and captured output: live state, not source
.pi/
research/
node_modules/
*.db
*.db-wal
*.db-shm
__pycache__/
*.pyc

# Local scripts
start-agents-a1.bat


# Upstream source clones for reference (not tracked)
reference/

# Machine-specific context calibration (see harness-config.local.json.example)
pi-config/harness-config.local.json

# Written when a bridge's handler runs against the REPO copy of its
# package.json, whose "pi-harness.root" is still the literal placeholder
# `TODO_SET_BY_RESTORE` until restore.py substitutes it on copy. Driving
# handlers in tests produced it once and it was committed by accident; the
# driver now runs in a temp cwd, and this keeps a stray one out of the repo.
TODO_SET_BY_RESTORE/

# C.A.S.E. Framework — task queues are agent workspace
02_Task_Queue/
*.case/
