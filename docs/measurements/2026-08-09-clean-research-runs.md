# 2026-08-09 乾淨重測:迴圈第一次在研究型 run 收斂,而先規劃只成立一半

**前提**:孤兒守衛生效(量測前檢查、`taskkill /T` 收樹)、逾時取自實測間隔分布(600 / 2400)。
**過程中 0 次孤兒警告。**

## 結果

| run | 輪次 | 工具 | 擋阻 | 推進注入 | 通知 | 認領前成功搜尋 | 認領後 | `output.md` | 終態 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 29 | 60 | 18 | **2** | ✅ | **0**(擋 10) | **22** | 1703 B | `REVIEW` |
| 2 | 39 | 72 | 30 | **6** | ✅ | **24**(擋 28) | 0 | 1597 B | `REVIEW` |

## 一、我原本要修的問題不存在

規劃裡排的下一項是「推進器在研究型 run 開不了口」,依據是三次 run 的 0 次注入。

**乾淨環境下:2 次與 6 次。推進器開得了口。**

那三次 0 是**污染環境的產物** —— 兩次被 180 秒門檻砍在生成中途,而當時有兩個我自己洩漏的孤兒
在搶模型伺服器。

> **復盤把它擋下來了。** 規劃時寫的那句「在污染環境量到的 0,不能當成設計新機制的依據」,
> 直接省掉一個為不存在的問題而做的機制。
> 2026-08-06 那次一整天的教訓是「在錯的基礎上動工,做得再仔細也是白做」——
> 這次是同一條規律,只是這次先量了。

## 二、迴圈第一次在研究型 run 收斂

**2/2 走到 `REVIEW`,任務包完整**(`planning.md`、`output.md`、`retro.md`、`action_log.jsonl`)。

對照:
* 2026-08-06:5 次 run **0 次**到 DONE,三次 ESCALATED
* 4b:走到 REVIEW 但**認領後不再搜**,交付物自陳未驗證
* 8/9 上一輪:撞上限、停在 `IN_PROGRESS`、無交付物

## 三、但「先規劃再開始」只成立一半

**run 2 在認領前成功搜了 24 次。**

那不是缺陷,是**設計上的取捨被實測看見**:退場機制擋滿 4 輪就讓路,否則一個學不會認領的模型
會被永遠鎖死。run 2 就是撐過了那 4 輪。

| | run 1 | run 2 |
|---|---|---|
| 認領前擋阻 | 10 | 28 |
| 認領前成功搜尋 | 0 | **24** |

**擁有者的原始抱怨在 1/2 的研究型 run 裡仍然會發生。**

## 四、所以下一個決定是門檻,而它有兩面

把 CLAIM 的退場從 4 輪加大,先規劃會更可靠 —— 但 4b 量到的另一面是:
**拒絕累積到一定量,模型會認定工具不可用並放棄整個能力。**

差別在於**現在有了階段轉換通知**(2/2 送達),那個副作用可能已經被拆掉。
**但那是推論,不是實測。** 要驗就是加大門檻再跑,並且**同時盯認領後的搜尋數**——
只看「認領前是否為 0」會漏掉代價,而那正是 4b 的教訓。

**在量到之前不動門檻。**
