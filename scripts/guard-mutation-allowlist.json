{
  "_comment": "Mutations that survive with an argument for why no test can tell the difference. A blank reason fails the run - see verdict() in scripts/check-guard-mutations.py. Verify each claim before adding one; an unverified entry is how a survivor list becomes a formality. Note that the object-literal form `block: true,` is NEVER equivalent - Pi reads the field - and belongs in a test, not here. Keys carry LINE NUMBERS and therefore expire: inserting code above an entry moves its site, the entry stops matching, and the survivor reappears looking new. That happened on 2026-08-10 to phase-gate.ts:287:38. When a survivor you recognise comes back, check whether an existing entry just moved before writing a second one.",
  "bash-containment.ts:47:10:true->false": "Type declaration, not code: `export interface ContainmentBlock { block: true; }`. TypeScript types are erased before the module runs, so the mutant is byte-identical at runtime and no test can distinguish it. Verified by reading lines 46-49.",
  "loop-detect.ts:44:10:true->false": "Same shape as bash-containment.ts:47 - `block: true;` inside an interface declaration, erased at runtime. Note the semicolon: the object-literal form ends in a comma and IS a real hole, which is why loop-detect.ts:73 is a test and not an entry here.",
  "queue-advancer.ts:81:12:true->false": "`paused?: true;` - an optional property in a type declaration. The `?:` syntax exists only in types, so this cannot reach runtime.",
  "task-queue-guard.ts:106:10:true->false": "`export interface QueueBlock { block: true; }` - a type declaration, erased at runtime. Verified by reading lines 104-108. The two object-literal sites in this file (the tool-first refusal and the Section 1 dual-track refusal) are covered by tests asserting the field, not by entries here.",
  "queue-advancer.ts:151:36:||->&&": "`if (typeof queueDir !== \"string\" || !queueDir) return null;` with && lets an empty string through, and the very next statement calls currentTask(queueDir) inside a try/catch that returns null on any throw. An empty path throws there, so both versions return null for every input - the observable behaviour is identical and no test can separate them. Verified by reading lines 150-158.",
  "approval.ts:71:13:||->&&": "`if (!text || text.length > MAX) return false;` with && lets an empty string through, and an empty string then matches neither approval pattern, so it returns false by the other door. Identical observable behaviour for every input - same shape as queue-advancer.ts:151. Verified by reading isHumanApproval end to end.",
  "phase-notice.ts:43:34:&&->||": "`typeof p === \"string\" && p` with || lets a non-string path reach resolve(), which throws TypeError for a number, object or array, and the surrounding try/catch turns that into `continue`. Every input reaches the same answer by a different route, so no test can separate them. Verified by reading writeTargets and the resolve() try/catch together.",
  "phase-gate.ts:104:10:true->false": "`export interface PhaseBlock { block: true; }` — a type declaration, erased before the module runs, so the mutant is byte-identical at runtime. Same shape as the entries for bash-containment.ts:47 and task-queue-guard.ts:106. Verified by reading lines 103-106.",
  "phase-gate.ts:152:36:||->&&": "`if (typeof queueDir !== \"string\" || !queueDir || !existsSync(queueDir)) return \"open\";` in phaseOf. With && a non-string truthy value passes the first clause, and every route below it fails open anyway: readdirSync throws inside tasks(), which catches and returns [], and `if (!all.length) return \"open\"` gives the same answer by the other door. Verified empirically — the mutant returns \"open\" for undefined, null, 5, {} and [] exactly as the original does, and tests/test_phase_tool_gate.py::TestFailsOpenOnWhatItCannotRecognise asserts those five.",
  "phase-gate.ts:152:49:||->&&": "Same clause as 152:36, the second `||`. With && an empty or missing path reaches tasks(), whose readdirSync throws into its own catch and returns [], and phaseOf then returns \"open\" from `if (!all.length)`. Same observable answer for every input, reached one line later. Verified against \"\" and \"/definitely/missing/queue\", both covered by TestFailsOpenOnWhatItCannotRecognise.",
  "phase-gate.ts:330:38:||->&&": "`if (typeof queueDir !== \"string\" || !queueDir) return null;` at the top of check(). With && a non-string truthy value continues instead of returning, and the very next statement calls phaseOf(queueDir), which fails open to \"open\", and `if (phase === \"open\") return null` returns the same null. The isolated clause is NOT equivalent; the function is, because the guard below it catches the same inputs. Verified for null, 5, {} and [] in TestFailsOpenOnWhatItCannotRecognise, which asserts check() returns null for each. NOTE: this entry was keyed 287:38 until 2026-08-10, when code inserted above it moved the line and the entry silently stopped matching — the survivor reappeared as if it were new. Allowlist keys carry line numbers and therefore expire whenever a file grows above them; re-verify rather than re-argue when one shows up again.",
  "phase-gate.ts:209:12:false->true": "Defensive `return false` on a path that cannot be reached: `resolve()` throws only for a non-string, and both arguments here are strings by the time they arrive — `queueDir` is type-checked at the top of check(), and the targets come from writeTargets(), which only ever returns strings. Flipping it to `true` would make the gate yield on every call, which every refusal test would catch — so the fact that none do is itself the evidence that the branch never executes. Verified by reading allWritesEscapeProject end to end.",
  "phase-gate.ts:217:14:false->true": "Defensive `return false` on a path that cannot be reached: `resolve()` throws only for a non-string, and both arguments here are strings by the time they arrive — the target is a string from writeTargets() and queueDir is type-checked at the top of check(). Flipping it to `true` would make the gate yield on every call, which every refusal test would catch — so the fact that none do is itself the evidence that the branch never executes. Verified by reading allWritesEscapeProject end to end.",
  "phase-gate.ts:211:21:false->true": "`if (!root) return false;` where root is the result of `resolve()`, which never returns an empty string for any input that did not throw — the throwing case is handled by the catch above. The clause is a belt on braces; flipping it changes behaviour only for a value that cannot occur. Verified by reading the two lines together.",
  "bash-containment.ts:297:17:||->&&": "`if (!tokens || tokens.length < 2) continue;` inside writeTargets. With && a null `tokens` reaches `tokens.length` and throws, and every caller wraps writeTargets in a try/catch that fails open — the same answer the original reaches by continuing. Probed 2026-08-10 against ten odd inputs (empty, bare operators, unbalanced quotes): the regex `match` returns null only for input the loop then skips anyway, so the throwing branch is unreachable in practice and no test can separate the two.",
  "bash-containment.ts:335:30:||->&&": "`isAbsolute(target) || /^[A-Za-z]:[\\/]/.test(target)` in escapesCwd. With && a POSIX-absolute path like `/tmp/x` takes the `resolve(cwd, target)` branch instead of `resolve(target)`, and `isScratch` is called with BOTH the resolved path and the raw token precisely so that case still reads as scratch — the raw `/tmp/` prefix matches either way. Windows drive paths satisfy both operands, so they are unaffected. Same observable answer for every input the guard sees.",
  "bash-containment.ts:339:12:false->true": "`return false` in escapesCwd's catch, taken only when `resolve()` throws. `resolve` throws for a non-string, and every target reaching it came from writeTargets, which returns strings only. Flipping it to `true` would make an unparsable target read as escaping and refuse the call — a fail-CLOSED change that every ordinary-work test would catch, so the fact that none do is the evidence that the branch never runs.",
  "bash-containment.ts:265:35:||->&&": "`if (typeof command !== \"string\" || !command.trim()) return [];` at the top of writeTargets. With && an empty or whitespace-only string continues instead of returning early, and every extractor below finds nothing in it — the regexes match no operator and `seg.trim().match(...)` returns null for each segment, so the function returns [] from the bottom instead of the top. Verified by calling writeTargets on \"\", \" \", \"|\", \";;\" and \"&&\": every one yields [] in both versions. Same shape as the entries for queue-advancer.ts:151 and approval.ts:71.",
  "bash-containment.ts:358:31:||->&&": "`if (typeof cwd !== \"string\" || !cwd) return null;` at the top of bashContainmentBlock. With && an empty-string cwd continues, and `escapesCwd` then returns false for every target because its own `if (!target || !cwd) return false` catches it — null by the other door. Probed 2026-08-10 with cwd undefined/null/5/{}/\"\": the mutant returns null for all five, exactly as the original does. Same shape as phase-gate.ts:330:38. NOTE: keyed 354:31 for about ten minutes on 2026-08-10 before four comment lines added above it moved the site — the second line-number expiry in one session. The keying scheme is the defect; until it changes, expect a familiar survivor to be a moved entry.",
  "goal-restate.ts:114:18:0->1": "Field initializer on `private acts = 0;` (and `sent` on the next line), not the assignment that matters. Every path that reads either counter runs through `begin()` first, which sets both to 0 unconditionally, and `afterToolResult` returns on a null goal before reading them — `goal` is null until `begin()` is called, so no caller can observe the initial value. The assignments inside `begin()` ARE observable and are covered: test_new_cycle_gives_the_full_run_up pins the acts reset and test_new_cycle_restores_the_budget pins the sent reset. Verified by reading GoalRestate end to end. NOTE: re-keyed 2026-08-10 after the file grew. The moved-entry matcher could not resolve these automatically because TWO entries share the same column and operator (acts and sent), and an ambiguous match fails loudly rather than guessing — which is the correct behaviour and the reason a hand re-key was still needed.",
  "goal-restate.ts:115:18:0->1": "Field initializer on `private sent = 0;` (and `acts` on the line before), not the assignment that matters. Every path that reads either counter runs through `begin()` first, which sets both to 0 unconditionally, and `afterToolResult` returns on a null goal before reading them — `goal` is null until `begin()` is called, so no caller can observe the initial value. The assignments inside `begin()` ARE observable and are covered: test_new_cycle_gives_the_full_run_up pins the acts reset and test_new_cycle_restores_the_budget pins the sent reset. Verified by reading GoalRestate end to end. NOTE: re-keyed 2026-08-10 after the file grew. The moved-entry matcher could not resolve these automatically because TWO entries share the same column and operator (acts and sent), and an ambiguous match fails loudly rather than guessing — which is the correct behaviour and the reason a hand re-key was still needed.",
  "phase-notice.ts:87:14:||->&&": "`if (!queue || !existsSync(queue)) return null;` with && lets a nonexistent queue path through, and the loop below then compares dirname(dirname(status.txt)) against it, which cannot match for a path that is not there. Both versions return null for every input. Same shape as queue-advancer.ts:151 and approval.ts:71. Verified by reading afterToolResult end to end. MOVED 2026-08-12: this entry was keyed 90:16 and the statement is now at 87:14 — the second time an allowlist key in this file has expired by drifting line numbers, which the header warns about. Re-verified rather than re-argued: driving claimedTaskDir and PhaseNotice.afterToolResult with a nonexistent queue path returns null from both.",
  "plan.ts:80:12:false->true": "`return false` in hasAnyPlan's catch, reachable only when statSync throws for a path existsSync just confirmed — a TOCTOU race where the plan file is deleted between the two calls, which no test can stage deterministically. The direction is the deliberate part and is argued in the comment above it: answering true would silence the routing advice, and that advice never blocks anything, so 'cannot tell' must fail toward advising. Flipping it to true would make every unreadable plan silence the router, which is the failure this whole change exists to remove. Verified by reading lines 71-82 with existsSync above it.",
  "advisory.ts:44:37:1200->1201": "`DEFAULT_DRAIN_BUDGET = 1200`, a character budget for text that rides along on a tool result. One character more or less changes what the model sees by one character and nothing else — there is no threshold at 1200 and no caller compares against it. A test asserting the literal would be a change-detector, not a behaviour test, and the behaviour that CAN be wrong is covered: packing stops at the budget, a block that exactly fits is not truncated, an oversized one is truncated rather than dropped, and the newline between advisories is counted. This is a calibration number in the sense of T-A2 — if it ever needs to move, it moves with a measurement, not with a test pinning it.",
  "ecc-payload.ts:142:21:&&->||": "`(section && typeof section === \"object\") ? section : null` in parseHookSpecificOutput. With || a truthy non-object — a bare string or number on stdout's hookSpecificOutput — is returned instead of null, and the caller reads exactly three properties off it: permissionDecision, permissionDecisionReason and additionalContext (lines 119-128). All three are undefined on a string or a number, so the deny branch is not taken and the advisory branch is not taken, which is the same outcome null produces one line earlier. `null` and arrays are unaffected: typeof null === \"object\" and typeof [] === \"object\", so both spellings agree on them. Verified 2026-08-12 by reading the three call sites and probing property access on a string.",
  "readability.ts:141:28:0->1": "`return m ? m[1].length : 0;` in indentOf, where m = line.match(/^(\\s*)/). That pattern is zero-width-capable and anchored at the start, so it matches EVERY string including the empty one — `match` never returns null here and the `: 0` arm is unreachable. Verified in node: '', 'x', '   x' and '\\t' all return a match object. A branch no input can reach cannot be distinguished by a test; deleting it would be the alternative, but it is the standard defensive form for `match` and reads as intentional.",
  "research.ts:142:60:300->301": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:179:33:8->9": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:179:37:1024->1025": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:179:44:1024->1025": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:181:33:256->257": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:181:39:1024->1025": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:174:23:<=-><": "`if (combined.length <= max) return combined;` in appendBounded. With `<`, a buffer of exactly `max` skips the early return and falls to the slice — which returns the same string: head takes slice(0, max) of a max-length string, and tail takes slice(len - max) = slice(0). Verified 2026-08-12 by running both spellings against ('abcde', '', 5) in each direction: 'abcde' from all four. The early return is a shortcut, not a decision.",
  "research.ts:10:34:5->6": "MAX_SUB_QUESTIONS, the cap on how many children a deep_research call may spawn. Five versus six changes cost, not correctness, and no caller compares against the literal — validateSubQuestions slices to it and its tests assert the slicing, not the number. A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test.",
  "research.ts:11:34:6000->6001": "A size constant, mutated by one. These are byte budgets for text nobody counts: MAX_CHILD_STDOUT (8 MiB, sized against the length that crashed V8), MAX_CHILD_STDERR (256 KiB, diagnostics that summarizeChildStderr trims again), and the 300-character default for that summary. One byte either way changes no decision and no output any caller compares — the boundaries that CAN be wrong are covered by tests: a summary that exactly fits is not trimmed, a trimmed one starts at its first character, and the rolling buffer keeps exactly its cap. Pinning the literals themselves would be a change-detector, not a behaviour test. Same argument as advisory.ts:44. Verified 2026-08-12 by reading every use of each constant.",
  "research.ts:16:61:true->false": "The RETURN TYPE annotation of validateSubQuestions: `: { ok: true; questions: string[] } | { ok: false; error: string }`. These are type literals, erased before the module runs, so the mutant is byte-identical at runtime — the same shape as bash-containment.ts:47 and task-queue-guard.ts:106. The RUNTIME flags in the function body are a different matter and are asserted by test_deep_research_bridge.py::test_the_ok_flag_says_what_happened, which was written for this sweep. Verified by reading line 16.",
  "research.ts:16:97:false->true": "The RETURN TYPE annotation of validateSubQuestions: `: { ok: true; questions: string[] } | { ok: false; error: string }`. These are type literals, erased before the module runs, so the mutant is byte-identical at runtime — the same shape as bash-containment.ts:47 and task-queue-guard.ts:106. The RUNTIME flags in the function body are a different matter and are asserted by test_deep_research_bridge.py::test_the_ok_flag_says_what_happened, which was written for this sweep. Verified by reading line 16.",
  "task-context.ts:313:18:0->1": "`private acts = 0;` — the field initialiser on TaskGoalRestate. Unreachable as a decision: every armed instance goes through `claimed()`, which sets acts = 0 before anything reads it, and an instance that never claimed has `goal === null`, so `afterToolResult` returns on the line above without touching the counter. The initial value can therefore only be observed by an instance that is both unarmed and asked for a restatement, which returns null either way. The threshold behaviour itself is covered by tests/test_task_context.py::TestTheTaskIsTheGoal, including that re-claiming the same task does NOT reset the count. Verified 2026-08-12 by reading claimed() and afterToolResult together.",
  "shape.ts:139:72:1->2": "Equivalent mutant. The 1 is the per-pattern score increment in classifyKind's reduce; doubling it scales every rule's score by the same factor, so the argmax and the `bestScore > 0` test are both unchanged. No input can distinguish them, which is what this list is for.",
  "dialect-residue.ts:102:23:8->9": "`for (let i = 0; i < 8; i++)` — the number of stacked closing tags stripped in one pass. The bound exists to stop an unbounded loop, not to draw a line at eight: the deepest shape the dialect can produce is three (`</atem:parameter></atem:invoke></atem:function_calls>`), and the measured session carried one. A test at nine WOULD separate 8 from 9, which is exactly why it is not written — it would pin an arbitrary constant and fail the next time the ceiling is raised, a change-detector rather than a behaviour test. Same argument as advisory.ts:44 and research.ts:179. The direction that CAN be wrong is covered: 8->7 was a live survivor until test_a_deep_stack_of_tags_is_fully_removed asserted eight tags are all removed, so the floor is pinned and only the unreachable headroom is not. Verified 2026-08-15 by re-running the exhaustive sweep on this module.",
  "no-plan-gate.ts:74:10:true->false": "`export interface NoPlanRefusal { block: true; }` — a type declaration, erased before the module runs, so the mutant is byte-identical at runtime. Same shape as the entries for bash-containment.ts:47, task-queue-guard.ts:106 and phase-gate.ts:104. The OBJECT-LITERAL `block: true,` at line 123 is NOT equivalent — Pi reads that field — and is covered by test_the_refusal_actually_carries_block_true rather than by an entry here, exactly as this file's header requires. Verified 2026-08-16 by reading lines 72-75 and re-running the exhaustive sweep."
}
